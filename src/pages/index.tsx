import { DefaultLayout } from "@/components/layout";
import { Heading } from "@chakra-ui/react";

const Home = () => {
  return (
    <DefaultLayout>
      <Heading>hello</Heading>
    </DefaultLayout>
  );
};

export default Home;
